bundle:
  name: nyc-taxi-weather-analytics

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405615297219439.19.azuredatabricks.net   # from terraform output databricks_workspace_url
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: ${workspace.current_user.userName}